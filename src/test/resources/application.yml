spring:
  datasource:
    url: jdbc:h2:file:./h2/integrationtest
    driver-class-name: org.h2.Driver
  jpa:
    database: h2
    hibernate:
      ddl-auto: update
  h2:
    console:
      path: /h2-console
      enabled: true
